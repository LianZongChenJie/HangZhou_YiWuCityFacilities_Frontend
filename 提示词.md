# 配套费征收审批系统 · 前端原型开发提示词

> 用途：本文件是开发前的协商稿。改这里即可，确认后再写代码。  
> 完整需求见 `readme.md`，精简需求见 `AIreadme.md`。  
> 定位：约 10 人内部办公工具，核心好用即可。

---

## 0. 先拍板（改这里）

| 项 | 当前约定 | 可选改法 |
|---|---|---|
| 缴款是否独立环节 | **要**：签发、到账分开，共 5 个业务状态 | 合并进签发，变成 4 步 |
| 技术形态 | **Vue3 若依风格 + Mock，不接 Java 后端** | 接若依 RuoYi 前后端 |
| 自动计费 | **表单内自动计算，允许人工改金额** | 先手填金额，不做公式 |
| 角色 | **5 个账号切换**：受理 / 审核 / 签发 / 办结 / 管理 | 再精简 |
| 导入 / 减免线下审批 / 多退少补自动核算 | **本期不做** | 指定要补哪一块 |
| 待办小红点 | **顶栏只做待办数量角标**（点开进待办列表，不做消息中心） | 连角标也不做 |
| 证号补录后是否重走审批 | **不重走**：补录写操作记录即可 | 按完整需求：补录后再走审核→签发→办结 |
| 审核/签发能否改表单 | **能**：审批过程中可改面积/金额等，改动写入审批记录 | 只能看、不能改，退回给受理岗改 |
| 已办件修改再审（3.7.1） | **本期不做**（仅草稿/退回件由受理岗改） | 受理岗改已流转件并再报审核 |
| 减免台账 | **统计里可筛「有减免」**，不做独立台账页 | 管理岗单独做减免台账页 |
| 关联单据 | **按工规证号列出同证号办件**（不做补缴/退费自动计算） | 完全不做关联 |
| 菜单结构 | **4 项**：工作台、办件管理（新建 / 待处理 / 办件查询）、统计报表、权限管理。菜单和默认页按四岗裁剪 | 见第 3 节 |
| 新建表前置材料 | **要**：三个勾选（不上传附件）。缴费表必勾；工规证复印件：未勾四证齐发时必勾；人防核定表：人防面积>0 时必勾 | 不做材料核验 |

改完上表后，下面正文以表格「当前约定」为准。

---

## 1. 给开发用的完整提示词

```text
你是资深前端工程师。请基于「若依 RuoYi-Vue3 + Element Plus」风格，编写一套【前端展示原型】，给约 10 人的内部办公小团队使用。目标是好用、流程清楚，不是完整政务系统。

【项目名称】
义乌市建设局城市基础设施配套费征收审批系统（原型）

【技术约束】
- 使用 Vue3 + Vite + Element Plus，页面布局、菜单、表格、表单、标签页风格对齐若依 RuoYi-Vue3（侧边栏 + 顶栏 + 多标签 TagsView）。
- 不接真实后端，不接若依 Java。列表/详情/审批/统计全部用 Mock 数据（可 mockjs 或本地 json）。
- 登录页提供角色切换：受理岗 / 审核岗 / 签发岗 / 办结岗 / 管理岗。切换后菜单与按钮权限立即变化。
- 中文界面。不做移动端。不要引入工作流引擎、BPMN、微前端。
- 代码结构清晰：views 按业务模块拆分，mock 数据集中，状态枚举集中定义。预置 8～12 条覆盖各状态的示例办件。

【本期只做这三块，其他一律不做完整功能】
A. 审批流程（核心）
B. 查询检索
C. 简单统计（筛选 + 汇总数字 + 导出 Excel）

明确不做（页面可留入口灰显或直接不出现）：
- Excel 历史台账批量导入（AIreadme 3.1.2）
- 减免线下审批全流程（仅允许录入时勾选「有减免」并填减免金额、政策依据）
- 变更/竣备多退少补的自动关联核算（业务类型可选；同工规证号只做列表关联，不算补缴/退费）
- 已办件修改后再报审核（AIreadme 3.7.1）；仅草稿和退回件可改
- 真实打印套打、电子签章、财政系统对接
- 独立消息中心；顶栏只显示待办数量角标，点击进入待办列表

【业务流程与状态机】
办件主流程（从左到右，详情页进度条必须展示）：
1. 待提交（草稿，仅受理岗可见，可选）
2. 待审核
3. 待签发
4. 待缴款
5. 待办结
6. 已归档

进度条颜色：已完成=绿，当前=黄，未到=灰。点击节点弹出该环节操作记录（操作人、时间、意见）。

操作规则：
- 受理岗：新建办件、保存草稿、提交审核、四证齐发时工规证号可空；仅看自己创建的办件；被退回的办件可修改后再次提交。
- 审核岗：核对规划面积、计费、项目信息；可改表单数据（改动写入记录）；通过 → 待签发；退回 → 待提交，必须填意见。
- 签发岗：① 确认开票/签发（填签发日期=开票日）→ 待缴款；② 确认到账（填到账日期）→ 待办结。审批过程中可改数据，改动写入记录。
- 办结岗：生成并打印「缴款审签单/办结回执」（同一张打印页，浏览器打印即可）、登记领取人/领取时间 → 已归档。已归档仍可查询、再次打印。
- 任意审批环节只允许当前角色操作当前状态的单据。
- 审核/签发/办结可查看全部办件。管理岗可查全部、看统计、导出。

退回只发生在审核环节。签发、缴款、办结不做复杂退回（保持原型简单）。

【四证齐发 · 证号补录】（AIreadme 3.2.1 / 3.4）
- 勾选「四证齐发」时工规证号可空，提交不受阻。
- 待办分类含「待证号补录」：四证齐发且工规证号仍为空的办件（流程中或已归档都算）。
- 补录入口：受理岗在办件详情点「补录工规证号」，填写证号后保存，写一条操作记录。
- 补录后不重新走审批（若改约定，以第 0 节为准）。
- 顶栏显示当前角色待办总数角标；工作台卡片按角色裁剪（见「四岗每天怎么用」），不要五类卡片摊给所有人。

【办件表单字段（录入/详情共用）】
项目信息：项目名称*、地块信息、资金来源（私营/政府/国有/集体/外资/其他+备注）、土地用途（多选：居住、住宅、商业、工业、教育、商务金融、城镇村道路、物流、物流仓储、其他+备注）
建设单位：名称*、联系人、联系电话
规划许可：工程规划许可证号（四证齐发可空，可后期补录）、地上建筑面积、地下建筑面积、地上住宅面积（即需求中的「住宅面积 / 缴款确认面积」输入项）、人防面积
业务标记（与 AIreadme 标记对齐，拆成类型 + 细分，避免一个勾选框里混两类）：
- 业务类型：初次 / 变更 / 竣备 / 历史补缴 / 减免（减免也可仅用「有减免」勾选，查询时能筛到即可）
- 项目细分（初次时）：新建 / 扩建 / 改建 / 拆复建 / 旧城改造拆建（对应需求「新建/改扩建/拆复建」）
- 勾选：四证齐发、有减免（减免金额、政策依据）
前置材料核验（新建/退回修改时勾选，不上传文件；详情只读展示已勾项）：
- 《义乌市城市基础设施配套费征收缴费表》（一式两份，建设单位盖章）*
- 《建设工程规划许可证》复印件（勾选「四证齐发」时可空）
- 《人防工程易地建设核实核定表》复印件（人防面积大于 0 时必勾）
提交审核时按上面三条校验；草稿不强制。审核岗办理页能看到勾选结果，便于核对「资料齐全」。
说明：列表里的「企业性质」= 表单「资金来源」，不要再单独做一套企业性质字段。
计费（自动计算，允许人工覆盖，覆盖后标「已人工修改」）：
- 地下住宅面积 = (地下建筑面积 - 人防面积) * (地上住宅面积 / 地上建筑面积)
- 住宅面积 = 地下住宅面积 + 地上住宅面积
- 非住宅面积 = [地上建筑面积 + (地下建筑面积 - 人防面积)] - 住宅面积
- 面积保留 2 位小数，四舍五入
- 应收总额 = 住宅面积 × 30 + 非住宅面积 × 80 - 政策减免金额
- 展示：住宅面积、非住宅面积、人防面积、减免金额、应收总额
审批意见：受理意见可默认「资料齐全，申报建筑面积与工规证一致，缴费金额无误」；审核意见可默认「申报材料符合要求，建筑面积与工规证一致，费用核算无误」；签发意见手填。

【四岗每天怎么用】（页面按这个习惯设计，不要做成所有人同一套首页）

受理岗（窗口，连续录件）：
- 受理岗：登录进工作台。最显眼是大按钮「新建办件」；卡片只有「退回待修改」「待证号补录」。
- 录件时先勾前置材料，再填项目信息。提交成功：主按钮「继续新建」，次按钮「返回」。窗口不关系统、下一家接着录。
- 退回件、补录从工作台卡片或「待处理」进。补录用弹窗填工规证号即可，不必重填整张表。
- 查「这证号办过没」：办件查询，工规证号放筛选第一位。
- 看不见统计报表、权限管理。侧栏「待处理」不要叫「审批」。

审核岗（对着材料逐件核）：
- 登录直接进「待处理」（待审核列表）。工作台可回，但不是主战场。
- 点行进办理页：面积、单价、应收金额置顶高亮，下面才是项目信息。底部固定「通过 / 退回」。
- 退回必须填意见。通过后停留在列表下一条，方便连审。
- 看不见新建、权限管理。统计报表可看（非主路径）。

签发岗（对财政系统，每日核到账）：
- 登录进「待处理」，默认页签「待缴款」（每日核到账是主业）；旁页签「待签发」（开票）。
- 待缴款列表要有：工规证号、项目、应缴金额、开票日。行内填「到账日期」+「确认到账」，不必先开整张详情。列表顶上放工规证号搜索，方便对财政系统。
- 待签发：填签发日期（=开票日）后确认。
- 看不见新建、权限管理。

办结岗（企业来领单）：
- 登录进「待处理」（待办结）。行内主按钮「打印回执」，打完在同一流程填领取人、领取时间并归档。
- 已归档再打一张：办件查询，不要再进待处理。
- 看不见新建、统计报表、权限管理。

【页面与菜单】
侧栏对四岗只出现自己用得到的项。统计报表、权限管理默认只给管理岗（审核/签发可看统计，但不作为入口强调）。

各角色看到的菜单：
- 受理岗：工作台、办件管理（新建办件 / 待处理 / 办件查询）
- 审核岗：工作台、办件管理（待处理 / 办件查询）、统计报表
- 签发岗：工作台、办件管理（待处理 / 办件查询）、统计报表
- 办结岗：工作台、办件管理（待处理 / 办件查询）
- 管理岗：工作台、办件管理（办件查询）、统计报表、权限管理

默认落地页：受理岗→工作台；审核/签发/办结→待处理；管理岗→工作台或统计报表。

1. 工作台 / 我的待办
   - 按角色展示，不要把五类卡片都摊给所有人
   - 受理岗：主按钮「新建办件」+ 卡片「退回待修改」「待证号补录」
   - 审核岗：卡片「待审核」
   - 签发岗：卡片「待缴款」（放前面）、「待签发」
   - 办结岗：卡片「待办结」
   - 顶栏角标=当前角色待办合计，点击回工作台
   - 卡片点击进入「待处理」对应页签

2. 办件管理
   2.1 新建办件（仅受理岗）
   2.2 待处理（原「审批」。待我处理，不是全量列表）
       - 受理岗：页签 退回待修改 / 待证号补录
       - 审核岗：待审核
       - 签发岗：待缴款（默认）/ 待签发
       - 办结岗：待办结
   2.3 办件查询（四岗都有；受理岗仅本人。唯一全量列表，含已归档）
       - 筛选第一位必须是工规证号（精确），其后：建设单位、项目名称、办件日期区间、业务类型、办件状态、金额区间
       - 列表列：项目名称、建设单位、工规证号、应缴金额、企业性质（资金来源）、土地用途、当前状态、开票时间、到账时间、办结时间
       - 行操作：详情；状态匹配当前角色时显示该岗主操作（提交/补录/通过/到账/打印等）
       - 详情：进度条 + 面积计费区 + 表单 + 审批记录 + 关联单据（同工规证号；无证号显示「暂无」）

3. 统计报表（管理岗主用；审核/签发可看；受理、办结不显示）
   - 先汇总再导出，不是第二张办件台账
   - 筛选：受理时间区间、办结时间区间（含开票、到账）、金额区间、状态、是否减免
   - 汇总：办件数量、应收合计、已到账合计、待缴款数量、减免金额合计、减免办件数量
   - 导出字段：工程规划许可证号、建设单位、资金来源、建设项目名称、土地用途、住宅面积、非住宅面积、人防面积、应缴城市基础设施配套费、签发日期、缴费到账日期、建设单位联系人、联系电话

4. 权限管理（仅管理岗）
   - 用户管理、角色管理。不做部门/菜单/字典/日志。

5. 缴款审签单 / 办结回执（不是菜单）
   - 办结岗从待处理行内「打印回执」打开；已归档从查询详情再打
   - 同一张：建设单位、项目名称、工规证号、住宅/非住宅/人防面积、缴费金额、受理/审核/签发意见、领取人、领取时间

【办理页按岗位收口】（同一详情组件，底部动作条按角色切换）
- 受理-新建/退回修改：整表可编辑，主按钮「提交审核」，次按钮「保存草稿」。提交成功出「继续新建」。
- 受理-补录：只弹工规证号，确认后写操作记录。
- 审核：面积与金额置顶可改；底部「通过」「退回」（退回必填意见）。办完回列表并定位下一条。
- 签发-开票：填签发日期，确认签发。
- 签发-到账：列表或抽屉填到账日期，确认到账。办完留下一条。
- 办结：先打印，再填领取人/领取时间，确认归档。办完留下一条。

【交互与体验（10人办公工具）】
- 列表默认按创建时间倒序；状态用 Tag 颜色区分。
- 提交、通过、退回、签发、到账、办结均二次确认。
- 办理完成后：受理岗提供「继续新建」；审核/签发/办结自动回到待处理并定位下一条。
- 必填校验清楚；四证齐发未勾选时工规证号必填。
- 面积变化时实时重算金额；人工改金额后不再被自动覆盖，提供「恢复自动计算」。
- Mock 数据要能演示完整一条链路：新建 → 审核通过 → 签发 → 到账 → 办结归档；另备：退回件、四证齐发缺证号（待补录）、已归档件、同工规证号的变更/竣备件（用于关联列表）、带减免金额的办件。
- 验收时分别用四个岗位账号走一遍各自主路径，确认登录落地页、菜单裁剪、主按钮符合该岗习惯。

【验收标准】
1. 四个岗位账号能把一条办件从受理走到归档；每岗登录后的第一屏就是该岗当天主工作。
2. 查询组合条件可用，工规证号精确查询放第一位；详情能看到进度和意见。
3. 统计页有汇总数字并能导出当前筛选结果。
4. 界面像若依后台，信息密度适合桌面办公，不要大屏可视化、不要花哨图表。
```

---

## 2. 本期范围对照（改范围时同步改第 1 节）

### 做

1. 四级审批主路径：受理 → 审核 → 签发 →（缴款确认）→ 办结归档
2. 办件录入与自动计费（可手改）
3. 待办列表 + 顶栏待办角标（分类：待审核、待签发、待缴款、待办结、待证号补录）
4. 四证齐发证号补录（受理岗，不重走审批）
5. 多条件查询 + 详情（进度条 + 审批记录 + 同证号关联列表）
6. 简单统计汇总 + 按清单导出 Excel（含减免合计数字）
7. 缴款审签单/办结回执浏览器打印；已归档可再查再打
8. 角色切换与按钮权限模拟；审核/签发审批中可改数据
9. 轻量权限管理（用户 + 角色，仅管理岗）
10. 菜单、落地页、主按钮按四岗裁剪（见第 3 节）
11. 新建表前置材料三项勾选（不上传附件）

### 不做

1. Excel 历史台账批量导入（3.1.2）
2. 减免线下审批全流程；不做独立减免台账页
3. 变更 / 竣备多退少补自动关联原单核算（只做同证号列表）
4. 已办件修改后再报审核（3.7.1）
5. 财政系统到账对接
6. 套打模板、电子签章
7. 消息中心 / 即时推送
8. 若依 Java 后端、工作流引擎

---

## 3. 状态、角色、菜单速查

### 状态

| 状态 | 谁处理 | 下一状态 |
|---|---|---|
| 待提交 | 受理岗 | 待审核 |
| 待审核 | 审核岗 | 通过→待签发；退回→待提交 |
| 待签发 | 签发岗 | 待缴款 |
| 待缴款 | 签发岗 | 待办结 |
| 待办结 | 办结岗 | 已归档 |
| 已归档 | — | 只查、可再打回执 |

### 角色权限

| 角色 | 能做什么 |
|---|---|
| 受理岗 | 录入、草稿、提交、补录工规证号、只看本人办件 |
| 审核岗 | 看全部、审批、修改数据、通过/退回 |
| 签发岗 | 看全部、审批、修改数据、签发开票、确认到账 |
| 办结岗 | 看全部、生成/打印缴款审签单（办结回执）、登记领取、归档 |
| 管理岗 | 全部查询、全部统计、按筛选看减免办件、导出全量 |

### 菜单树（按岗裁剪，不要所有人同一套）

完整树（管理岗能看见的上限）：

```
工作台 / 我的待办
办件管理
  ├─ 新建办件      （仅受理岗）
  ├─ 待处理        （待我处理；不要叫「审批」）
  └─ 办件查询      （唯一全量列表）
统计报表           （管理岗；审核/签发可看）
权限管理           （仅管理岗）
  ├─ 用户管理
  └─ 角色管理
```

| 角色 | 侧栏 | 登录进哪 | 当天主路径 |
|---|---|---|---|
| 受理岗 | 工作台、办件管理（新建/待处理/查询） | 工作台 | 新建 → 提交 → 继续新建；空闲时补录、改退回件 |
| 审核岗 | 工作台、办件管理（待处理/查询）、统计 | 待处理 | 打开待审核 → 核对面积金额 → 通过/退回 → 下一条 |
| 签发岗 | 工作台、办件管理（待处理/查询）、统计 | 待处理（默认待缴款） | 对财政系统填到账日期确认；另处理待签发开票 |
| 办结岗 | 工作台、办件管理（待处理/查询） | 待处理 | 打印回执 → 填领取人 → 归档；补打走查询 |
| 管理岗 | 工作台、查询、统计、权限 | 工作台或统计 | 查数、导出，不参与审批 |

| 入口 | 干什么 |
|---|---|
| 工作台 | 受理岗放大「新建」；其余岗只放自己的待办卡片 |
| 待处理 | 该岗收件箱，连办下一条 |
| 办件查询 | 用工规证号找件；办结岗补打回执 |

---

## 4. 计费公式（录入页必须实现）

单价：住宅 30 元/㎡，非住宅 80 元/㎡。人防面积不计征。

```
地下住宅面积 = (地下建筑面积 - 人防面积) × (地上住宅面积 / 地上建筑面积)
住宅面积     = 地下住宅面积 + 地上住宅面积
非住宅面积   = [地上建筑面积 + (地下建筑面积 - 人防面积)] - 住宅面积
应收总额     = 住宅面积 × 30 + 非住宅面积 × 80 - 政策减免金额
```

面积保留 2 位小数，四舍五入。地上建筑面积为 0 时，地下住宅面积按 0，避免除零。

---

## 5. 修改记录

| 日期 | 说明 |
|---|---|
| 2026-08-13 | 初稿。MVP = 审批流程 + 查询检索 + 简单统计。若依 Vue3 风格 + Mock。 |
| 2026-08-13 | 对照 AIreadme 查漏：补录操作、待办角标、查询减免、企业性质=资金来源、同证号关联列表、审核签发可改数据、审签单=回执、归档后再打。导入/减免全流程/自动多退少补/修改再审仍不做。 |
| 2026-08-13 | 菜单定为 5 项：工作台、办件管理（新建/审批/办件列表）、办件查询、统计报表、权限管理。一级不叫「办件受理」。 |
| 2026-08-13 | 合并重复：「办件列表」并入「办件查询」，查询收到办件管理下。侧栏改为 4 项。审批保留为待我处理，与全量查询不重复。 |
| 2026-08-13 | 按四岗操作习惯改：审批改名待处理；登录落地页分岗；工作台内容分岗；签发默认待缴款且列表内确认到账；办结列表内打印；审核面积金额置顶；受理提交后继续新建。 |
| 2026-08-13 | 补「怎么检查」：三文件对照表 + 你我各自的检查步骤。修正工作台卡片与分岗裁剪的自相矛盾。 |
| 2026-08-13 | 新建表增加前置材料三项勾选（缴费表、工规证复印件、人防核定表），不上传附件。 |

---

## 6. 三文件对照（AIreadme 为主，readme 补规则）

检查规则：
- **对齐**：提示词能做出需求写的结果
- **降级**：需求有，提示词做了简化，且第 0 节有拍板
- **缺口**：需求有，提示词没写，也没说不做（这才是漏）
- **超出**：提示词有、需求没有（一般是交互优化，不算错）
- **以谁为准**：功能条目以 `AIreadme.md` 为准；计费公式、状态机、四证齐发补录后是否重审，以 `readme.md` 第二章为准（AIreadme 没抄那一章）

### 6.1 AIreadme 逐条

| 条目 | 结论 | 说明 |
|---|---|---|
| 核心1 四级审批 | 对齐 | 并拆出待缴款（来自待办分类和 readme 2.4） |
| 核心2 自动计费 | 对齐 | 公式在第 4 节，可手改 |
| 核心3 多类型+多退少补闭环 | 降级 | 类型能选；不算补缴/退费。第 0 节已拍板 |
| 核心4 待办/补录/权限 | 对齐 | 待办+补录+分岗权限；小红点降为角标 |
| 3.1.1 录入字段 | 对齐 | 企业性质=资金来源；标记拆成类型+细分 |
| 3.1.2 Excel 导入 | 降级 | 明确不做 |
| 3.2.1 四岗职责 | 对齐 | 四岗主路径都有对应按钮 |
| 3.2.2 进度条绿黄灰 | 对齐 | |
| 3.3.1 回执字段 | 对齐 | 审签单=办结回执 |
| 3.3.2 到账后办结、归档可查可打 | 对齐 | |
| 3.3.2 变更/竣备关联主台账 | 降级 | 同证号列表，不算费用 |
| 3.4 小红点 + 五类待办 | 降级 | 角标+分岗卡片，不做消息中心 |
| 3.5.1 查询条件 | 对齐 | 含减免；工规证号第一位 |
| 3.5.2 列表/详情/关联单据 | 对齐 | |
| 3.6 统计筛选与导出字段 | 对齐 | |
| 3.7.1 受理修改再报审核 | 降级 | 明确不做，仅草稿/退回可改 |
| 3.7.2 分岗权限、审签单、减免台账 | 对齐/降级 | 权限与审签单对齐；减免台账降为统计筛选 |

**AIreadme 相对提示词：没有未登记的缺口。** 差别都在第 0 节「降级」里。

### 6.2 readme 有、AIreadme 没有（容易漏）

这些不在 AIreadme 正文里，但 `readme.md` 第二章写了。提示词目前：

| readme 条目 | 结论 | 说明 |
|---|---|---|
| 2.1 计费公式与单价 | 对齐 | |
| 2.2.1 前置纸质材料（缴费表、工规证复印件、人防核定表） | 对齐 | 新建表三项勾选，不上传；提交时按四证齐发/人防面积校验 |
| 2.2.2 变更自动提取已缴、算补缴/退费 | 降级 | 与核心3同一拍板 |
| 2.2.3 补录后再次走四级审批；定时催补录 | 降级 | 第 0 节：不重走、无定时任务 |
| 2.3 待线下审批挂起、未完成不能缴款 | **缺口** | 有减免勾选和金额，没有「待减免审批」状态 |
| 2.4.1 状态名「待受理」「待办结盖章」 | 超出/更名 | 提示词用待提交、待办结，意思相同 |
| 2.4.2 办结后再补录，标记「四证齐发待二审」 | 降级 | 补录不改状态、不二审 |

### 6.3 提示词超出需求（不算错）

若依壳、Mock、登录角色切换、继续新建、列表内确认到账、分岗落地页、权限管理菜单、待提交草稿。这些是办公习惯，需求没写细。

---

## 7. 以后怎么检查（你做 / 我做）

### 你怎么检查（约 20 分钟，不靠记忆）

1. 打开 `AIreadme.md`，从「核心目标」到 3.7.2，**每一条**在提示词第 0 节或第 6 节找到对应行。找不到 = 缺口。
2. 只看第 0 节「当前约定」和第 2 节「不做」：这是主动砍掉的清单。问自己「上线给 10 人用，砍了会不会办不下去」。
3. 用四岗各走一遍纸面路径（不需要代码）：
   - 受理：新建→提交→（退回）再提交→补录证号
   - 审核：待审核→核对面积金额→通过 / 退回
   - 签发：待签发开票→待缴款填到账
   - 办结：打印回执→填领取人→归档→查询里再打一张
4. 若某条你认为「必须做」却在「不做」里，改第 0 节，不要直接改代码想法。

建议你亲自拍板的还剩 2 句：
- 有减免时要不要「待减免审批」挂起？（readme 2.3，AIreadme 无）
- 补录证号后要不要重走审批？（readme 要，AIreadme 没写，提示词不重走）

已拍板：新建表加前置材料三项勾选（不上传附件）。

### 我怎么检查

1. 把 AIreadme 拆成原子条目（字段、按钮、状态、权限、导出列），与提示词做表，标 对齐/降级/缺口。
2. 再扫 `readme.md` 第二章，找出 AIreadme 没摘进去的规则（本次已列入 6.2）。
3. 扫提示词自身是否打架（例如先写「五类卡片全展示」后又写「按岗裁剪」——本次已改）。
4. 写代码后按第 7 节四岗路径做一遍页面验收，对照 AIreadme 3.1.1 字段和 3.6 导出列打勾。

你改需求时：先改 `AIreadme.md` 或第 0 节，再说一声「再对照一次」，不要只在聊天里改口径。

